package com.ilyzs.basecompat.util;

import android.os.Environment;

import com.ilyzs.basecompat.base.CompatBaseApplication;

/**
 * Created by zhangshu on 2017/11/20.
 */

public class ConfigUtil {
     /*   if (Environment.MEDIA_MOUNTED.equals(Environment.getExternalStorageState()) || !Environment.isExternalStorageRemovable()) {
        cachePath = CompatBaseApplication.getInstance().getExternalCacheDir().getPath();
    } else {
        cachePath = CompatBaseApplication.getInstance().getCacheDir().getPath();
    }*/

}
